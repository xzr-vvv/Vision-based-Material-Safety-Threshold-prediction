# V-MaST 项目阶段总结报告

日期：2026-08-22
项目：V-MaST（基于视觉的材料安全阈值预测 / Vision-based Material Safety Threshold prediction）
报告范围：2026-08-18 至 08-22 数据治理、离线评测与首批交付工作
协作背景：与机械臂 Gen3 侧合作方共同推进 SafetyVTLA 方案（视觉先验 → 力区间 → PI0.5 策略 → 触觉在线修正 → 真机闭环），本方负责视觉与数据一侧

## 一、当前所处位置

SafetyVTLA 全链路中，本方负责的前段（视觉先验数据底座）已按 A 级（离线视觉先验）要求完成：数据集定稿、力值真实性治理、按族离线评测基线、首批交付包组装全部就绪。P0 任务仅剩一项外部依赖——合作方机器上的 SafetyVTLA 预测器代码尚未拿到。B/C 级（在线 shadow、真机闭环）所需数据未启动，预测器与执行器保持 disarmed。

## 二、已完成任务

### 2.1 RGB 数据集定稿

- 最终规模 181 物体 / 1847 图：轻脆 30、重脆 30、刚体 61、柔性 60，满足每类 ≥30 的要求
- 129 个 ExpForce 物体全部保留（含 62 个单图物体），E 系本地分类与 ExpForce 官方分类全量比对零不一致（61 刚体 / 57 柔性 / 11 易碎，轻脆 3 + 重脆 8 无损映射易碎 11）
- 41 个不符合要求物体移入 `RGB_dataset\_quarantine\`（21 个 YCB + 20 个仅有近似转移力值的 FRA），可恢复、不交付
- 数据集校验脚本 `dinov3_dual\check_dataset.py` 重写为 stdlib csv 版本（E 盘环境无 pandas），复跑多样性检查与完整性校验通过
- 模型路线确认单流 DINO（DINOv2 ViT-L 冻结 + L1/L2 层级头 + 逐叶力回归），双流方案废弃；当前阶段为 RGB 数据建设期，训练工作暂停待恢复

### 2.2 力值真实性治理

这是本阶段工作量最大、也最重要的部分。核心原则：力值列只允许别人的实测值，用户不自测、不估算、不静默换算。

- 删除 force_map.csv 中 254 行估算 f_max（类别系数 × f_min 推算）与 52 行近似转移 f_min，全部留 `[0821删]` 审计标记
- 清理后 labels.csv 力值构成：
  - f_min 共 149 个物体 = ExpForce 本物实测 129 + 同型实测转移 20
  - f_max 共 44 个物体 = 文献/国标实测值换算（平板压缩等加载方式）
- 力值语义显式化：主语义为两指法向力之和，另设 `f_min_single_N` 列按 F_single = F_total/2 显式换算；f_max 换算注明来源与规则（如"文献实测 42N(平板压缩)×2 换算 = 84N(两指合力，非直接实测)"）
- f_max 真相核查结论：44 个 f_max 中 39 个是文献单点实测的 ×2 语义换算，5 个抓持语义但单指/合力未确认（保守按合力处理）；全数据集没有任何物体有直接实测的两指合力 f_max。该结论已写入 labels.csv 备注列与数据集 README
- 加载方式适用性论证：本项目夹爪为两指 + 传感器平板指面，与文献平板压缩属同一加载类，×2 换算成立；剩余裕度考虑加载速率与各向异性方向（鸡蛋赤道弱于长轴），保留 1.2–1.5 倍安全系数，已写入 README 加载方式警示章节

### 2.3 族体系统一与切分防泄漏

- 合并 15 组同型异族（蛋 3 族归 egg、苹果 4 族归 apple、番茄 5 族归 tomato、玻璃杯 3 族、高脚杯 4 族等），族数 128 → 103
- 解决的问题：同型物体（如褐蛋/白蛋）分属不同族时，按族切分会让同型物体横跨训练/测试集，造成泄漏性虚高
- labels.csv 与 objects_模板.csv、objects_已有安全阈值.csv 三文件族口径同步

### 2.4 阈值表物理量增补

- 41 个物体的 mass_kg 从 ExpForce 实测质量三级模糊匹配补齐（精确规范化 → 前缀 → 相似度），dimensions/volume/density 按典型尺寸估算（±30% 口径标注）
- 修正方糖盒 RIG055 尺寸参数错误导致的密度异常（3559 → 840 kg/m³）

### 2.5 按族离线评测（基线版）

- 脚本 `数据采集准备\evaluate_visual_force_prior_offline.py`，按族留一法（LOFO）切分，报告 `training_log\visual_force_prior_offline_20260821.md`
- 三个基线结果（单指 MAE）：全局均值 0.407 N、类别均值 0.301 N、物理先验 mg/μ 8.128 N
- 结论：F_min 下界由表面材质与接触几何决定，质量几乎不携带信息；理论公式路线不可行，必须走视觉语义路线——这是整个视觉先验方案的实验立论依据
- 类别均值 0.301 N 定义为后续视觉模型必须超越的及格线（它不看图，只按易碎/柔性/刚体取类均值）

### 2.6 分类口径闭环

- 比对脚本 `check_flag_consistency.py` 四源全量比对（阈值表手填 flags / 本地模板 / labels.csv / ExpForce 官方）
- 阈值表 4 处不一致处置：FRA016 纸杯、FRA039 塑料杯为过时标记，已对齐柔性（备份 .bak_before_flag_unify）；FRA048 Pocky 盒（本地轻脆 vs 官方刚体）、SOF053 袋装软糖（本地柔性 vs 官方刚体，参照 E055 实为瓶装）为真实语义分歧，保留双口径并显式标注
- 统一口径拍板为双轨制：本地四分类是训练/评测唯一标签口径，ExpForce 三分类仅作力值参照来源口径；冲突时本地优先（安全侧保守）且必须显式标注，禁止静默覆盖

### 2.7 首批交付包

`safety_vtla_data_delivery_20260821\`（1.7 GB），对应《数据要求 v1》§4 第一批第 1 项：

| 内容 | 规格 |
|---|---|
| objects.csv | 181 行，§1.1 全字段 + 力值标签全字段（f_min 149、f_max 44，逐物体来源/语义/换算标注）|
| captures.csv | 1847 行，真实像素尺寸（PNG 头解析）+ 逐图来源/URL/许可证 |
| splits.json | 按族 md5 确定性 70/15/15，强制每 split 覆盖 4 叶类；train 111 物体/1189 图、calibration 36/372、test 34/286，零族泄漏（已验证）|
| images/ | 1847 张原图按物体分目录 |
| README.md | 来源、许可证、单位语义、缺失字段申报、弱先验声明、§6 自检表 |
| checksums.sha256 | 1851 条全文件校验和 |

诚实申报的缺失：采集元数据（session/相机/时间戳/距离/视角/光照）因来源异构全部留空未编造；尺寸/体积/密度仅 14 个物体有值（典型值 ±30%）；质量 143 个有值（129 实测 + 14 同型参照）。

### 2.8 协作与检索定论

- SafetyVTLA 仓库代码（predict_visual_grasp_force.py、agent_rule.md、tests/）全盘搜索定论：E/C/D 盘无限深度、GitHub 账号、两个微信号传输记录、回收站 15.3 万条目均无——仓库从未在本机，位于合作方机器；本机仅有 4 份微信接收文档（D:\微信\...\2026-08\）
- GitHub 仓库改名 xzr-vvv/Vision-based-Material-Safety-Threshold-prediction（中文名入描述），合作者已授 admin 权限

## 三、修正的错误

| 错误 | 修正 | 留档 |
|---|---|---|
| 估算力值冒充实测：254 行 f_max 由类别系数 × f_min 推算、52 行 f_min 近似转移，曾混入力值列 | 全部删除，只保留别人实测值；无实测的物体力列留空仅作分类用途 | force_map.csv `[0821删]` 审计标记 |
| 静默单位/语义换算：鸡蛋 84N 曾被当作实测呈现，实际是文献单点 42N 的 ×2 换算 | 换算规则、原始值、加载方式全部显式标注；44 个 f_max 逐个写明"非直接实测" | labels.csv note 列、README |
| 阈值表过时 flags：FRA016/FRA039 纸杯塑料杯仍标易碎，与模板、labels、ExpForce 官方三方（均柔性）不符 | 对齐柔性，物理依据为失效模式是压瘪变形非脆断 | .bak_before_flag_unify 备份 |
| 同型异族切分泄漏：褐蛋/白蛋、各苹果变体分属不同族，按族切分时同型横跨 split | 15 组合并统一族体系（128→103） | unify_families.py |
| RIG055 方糖盒密度 3559 kg/m³（尺寸参数填错） | 尺寸修正后密度 840 kg/m³ | enrich_thresholds_csv.py DIMS |
| 历史性教训（更早已改）：随机切分导致物体族泄漏、准确率虚高；刚性柔性合并非脆类掩盖力值系统性变差 | 全面改为按族隔离切分；L2 保持四叶类不合并 | project_memory 教训条目 |

## 四、下一步计划

立即可做（主动权一次沟通）：

1. 发送交付包 + 向合作方索取 SafetyVTLA 仓库代码（predict_visual_grasp_force.py、agent_rule.md、tests/），并顺带提出 P1/P2 数据需求
2. 拿到代码后接入 `evaluate_visual_force_prior_offline.py` 替换三个基线，目标超越类别均值 MAE 0.301 N
3. 评测协议完善：最差族 MAE、危险方向错误（预测高于真值）单独统计、区间覆盖率与区间宽度、分叶类报告——安全场景下这些比总体平均更重要

外部依赖（按缺口整理 §7 优先级）：

| 优先级 | 任务 | 依赖方 | 本方角色 |
|---|---|---|---|
| P1 | 触觉因果时序 episode + compliant_grid（sponge/soft_tube）采集 | 合作方 | 接收数据 |
| P2 | Gen3 腕相机 RGB 跨相机样本 | Gen3 硬件侧 | 样本到位后做跨相机校准/域适应 |
| P3 | 同构 F_max 破坏性实测 + Robotiq 命令百分比→单指牛顿标定 | Gen3 + Sensor3D 真机 | 接收并换算 |
| P4 | ROS2 bringup + R0–R10 晋级记录 | §4.3 全部就位 | 联调 |

红线不变：P3/P4 数据到位前 predictor 与 executor 保持 disarmed；本方任何离线结果不得表述为"Gen3 视觉→安全阈值"；ExpForce 的 5×/3×/2× 类别系数禁止用作 F_max 监督。

## 五、经验教训

**1. 别人的实测数据不一定适用于自己，必须具体情况具体分析。**

这是本阶段最重要的一条。ExpForce 的力值是真机实测、协议规范（每物体 3 次试验、0.25 N 起步自适应收紧、滑移才加力、举升 5 cm 验证、取中位数），数据本身完全可信——但它是在 Franka + FORTE 软鳍指平台上测的。本方是 Gen3 + Robotiq 2F-85 + 传感器平板指，接触面软硬、摩擦特性、贴合方式都不同，同一个鸡蛋的"最小力"换平台就是另一个数。所以 0.25 N 是"它的夹爪的答案"，不是"本方夹爪的答案"，绝对值必须等 P3 同构标定换算后才能上真机。

而且"具体情况具体分析"要细到数据集内部的逐项判断，不能整个数据集一刀切：ExpForce 的 RGB + F_min 半边可用（换算语义后）；F_max 半边不可用（类别系数估算，audit 明文禁止当真值）；深度半边根本不存在。同一个来源里，可信与不可信是按字段、按语义、按加载方式逐项划界的。

**2. 实测、换算、估算必须三级分层，来源永不静默。**

84 N 鸡蛋事件的教训：一个数是"直接实测"还是"文献实测后 ×2 换算"还是"系数推算的估算"，可靠性完全不同，混在一个列里就等于污染。治理后的标准是：每个力值都带来源（本物实测/同型转移/文献换算）、语义（单指/两指合力）、换算规则三重标注；哪怕结论是"全数据集没有一个直接实测的两指合力 f_max"，也要如实写出来。宁可留空，不可编造。

**3. 理论公式不是真值，操作性定义才是。**

物理先验 mg/μ 在 41 个物体上 MAE 8.128 N，惨败于两个统计基线。原因是它预测的是"刚体点接触、单一摩擦系数下的恰好不滑落力"，而真值是"软指贴合、自适应收紧的实际抓持结果"——真实接触远比理论慷慨。这反过来定义了什么是工程意义上的 F_min：不是教科书公式，而是"真机按协议抓得住、举得起的力"。同时它也证明质量几乎不携带 F_min 信息，材质外观才携带——这就是视觉路线的立论。

**4. 评测先立诚实的尺子，再看模型。**

不看图的类别均值（0.301 N）是最笨也最公平的及格线：无参数、无训练、不可能过拟合、任何人重跑同数。视觉模型打不过它就说明视觉是多余的。另外安全场景的指标纪律：最差族比平均值重要（鸡蛋上偏 3 N 就是碎一个鸡蛋）、危险方向的错误（预测偏高）要和保守方向分开统计、缺失的元数据如实申报而不是事后补写。

**5. 同型不等于同物。**

同型转移（20 个 FRA 的 f_min）天然带噪声；瓶装软糖（E055，刚体的是瓶子）和袋装软糖是两个物体。同型参照能用，但必须显式标注参照物和差异点，且在评测中与本物实测分账。

**6. 工程环境约束也是要记录的知识。**

E 盘 Python 环境无 pandas 且沙箱禁止安装（stdlib csv 重写所有脚本）；真实 Python 是 E:\python.exe（3.14，可加载 E:\Lib\site-packages 的 torch）；TRAE 沙箱 python 是虚拟机 3.10 装不了 cp314 扩展。这些约束每次踩坑都耗时，记入项目记忆后不再重复。

## 附：关键文件索引

| 文件 | 内容 |
|---|---|
| `safety_vtla_data_delivery_20260821\` | 首批交付包（1.7 GB，可直接压缩发送）|
| `数据采集准备\evaluate_visual_force_prior_offline.py` | 按族离线评测脚本（预测器可插拔，待接真模型）|
| `数据采集准备\build_delivery.py` / `write_readme_checksums.py` | 交付包构建脚本（可复现）|
| `数据采集准备\check_flag_consistency.py` + `fragile_flag_不一致清单与统一口径_20260821.md` | 分类口径比对与拍板 |
| `数据采集准备\unify_families.py` / `enrich_thresholds_csv.py` | 族统一 / 物理量增补 |
| `training_log\visual_force_prior_offline_20260821.md` | 离线评测报告（基线版）|
| `SafetyVTLA_缺口与可用资源整理_20260821.md` | 缺口地图（本报告的上游文档）|
| `RGB_dataset\labels.csv` / `force_map.csv` / `provenance.csv` | 数据集三大核心表 |
